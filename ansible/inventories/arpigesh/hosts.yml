---
# Arpigesh - Primary site (home)
# Usage: ansible-playbook playbooks/site.yml -i inventories/arpigesh/hosts.yml

all:
  children:
    # Device type groups
    standalone:
      hosts:
        home:
          ansible_host: home  # Tailscale MagicDNS

    cluster:
      hosts: {}
        # TBD: DeskPi Super6C nodes
        # node1:
        #   ansible_host: node1
        # node2:
        #   ansible_host: node2

    # Stack groups (for targeted playbooks)
    homebridge_stack:
      hosts:
        home:

    # Future stacks
    # k3s_stack:
    #   hosts:
    #     node1:
    #     node2:
