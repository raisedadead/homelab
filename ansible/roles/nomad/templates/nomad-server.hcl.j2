datacenter = "dc-publish"
data_dir   = "/opt/nomad"

server {
  enabled          = true
  bootstrap_expect = 3

  server_join {
    retry_join = ["rpi-cm4-01.local","rpi-cm4-02.local","rpi-cm4-03.local"]
    retry_max = 3
    retry_interval = "15s"
  }
}
