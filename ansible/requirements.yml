---
collections:
  # Core utilities (shutdown, lineinfile, etc.)
  - name: community.general
    version: ">=10.0.0"

  # Docker management with proper idempotency
  - name: community.docker
    version: ">=4.0.0"

  # POSIX modules (authorized_key, sysctl, etc.)
  - name: ansible.posix
    version: ">=2.0.0"

roles:
  # Docker CE installation
  - name: geerlingguy.docker
  - name: geerlingguy.pip
