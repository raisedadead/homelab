---
collections:
  # Used by play--00-power.yml (shutdown) and play--20-pve-subscription.yml (lineinfile)
  - name: community.general
    version: "==12.1.0"

  # Unused - no playbooks currently use docker modules from this collection
  # - name: community.docker
  #   version: ">=3.0.0,<5.0.0"

  # Unused - no DigitalOcean inventory or modules in use
  # - name: community.digitalocean
  #   version: ">=1.27.0,<2.0.0"

  # Unused - no POSIX-specific modules in use
  # - name: ansible.posix
  #   version: ">=2.0.0,<3.0.0"

  # Unused - no Grafana provisioning playbooks
  # - name: grafana.grafana
  #   version: ">=5.7.0,<6.0.0"

  # Unused - no K3s deployment playbooks
  # - name: https://github.com/k3s-io/k3s-ansible.git
  #   type: git
  #   version: 1.1.1

roles:
  # Unused - no rclone backup playbooks
  # - name: rolehippie.rclone
  #   version: "v2.8.1"

  # Used by play--10-docker.yml
  - name: geerlingguy.docker
  - name: geerlingguy.pip

